# Copy this into Streamlit Cloud → App settings → Secrets
# https://share.streamlit.io → your app → Manage app → Settings → Secrets

OPENWEATHER_API_KEY = "paste_your_openweather_key_here"

# Optional — leave commented unless needed
# HOPSWORKS_API_KEY = ""
# HOPSWORKS_PROJECT = "your-hopsworks-project-name"
# FEATURE_STORE = "local"
